// Set(it is iterable)
// store data
// sets algo have its own methods
const arr1 = [000, 111, 222];
const numbers = new Set([1, 2, 3, 4, 4]); // duplication remove


numbers.add(10);
numbers.add(10);
numbers.add(10);
console.log(numbers);

if (numbers.has(10)) {
    numbers.add(arr1);
}
else {
    number.add(1);
}


for (const iterator of numbers) {
    console.log(numbers);
}
